<h2>Dettaglio articolo</h2>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Nome</th>
            <th>Prezzo</th>
            <th>Descrizione</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>@Model.Id</td>
            <td>@Model.Nome</td>
            <td>@Model.Prezzo</td>
            <td>@Model.Descrizione</td>
        </tr>
    </tbody>
</table>

<div>
    <a href="@Url.Action("Index", "Admin")" class="btn btn-default">Torna alla lista</a>
</div>
